package toy.language;

public enum Token implements Sentential {
	
	NUM, PLUS, MULT, AND, OR, GT, LT, EQ, LPAREN, RPAREN, TRUE, FALSE, 
	NOT, SKIP, ASSIGN, SEMI, IF, THEN, ELSE, WHILE, DO , LBRACES,
	RBRACES, VAR, $, NULL;

	
}
